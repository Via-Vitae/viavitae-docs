# Local preview: Hugo development server.
# Usage: docker compose up → http://localhost:1313

services:
  hugo:
    image: klakegg/hugo:ext-alpine
    command: server --environment staging --bind 0.0.0.0
    ports:
      - "1313:1313"
    volumes:
      - .:/src
    restart: unless-stopped
